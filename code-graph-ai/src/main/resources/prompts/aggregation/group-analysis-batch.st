# 流程说明书分批聚合分析

## 任务描述
请分析以下流程说明书列表，根据功能相关性将它们分组聚合。这是第 {batchIndex} 批（共 {totalBatches} 批）的分析。

## 当前批次说明书列表
{summariesJson}

{if(!isFirstBatch)}
## 之前批次的聚合结果
以下是之前批次已经生成的聚合分组，每个分组都有唯一的groupId：
{previousGroupsJson}

### 重要说明
- 你需要考虑当前批次的说明书是否可以归入已有的聚合分组
- **如果当前批次的说明书与已有分组高度相关，应该合并到现有分组**：
  - 在返回结果中设置 `historicalGroupId` 字段为对应的历史分组ID
  - 不要设置 `groupId` 字段（系统会自动处理）
- **如果当前批次的说明书形成新的业务模块，创建新的分组**：
  - 不要设置 `historicalGroupId` 字段
  - 不要设置 `groupId` 字段（系统会自动分配）
- 请在分组描述中说明与已有分组的关系（如果有的话）
{endif}

## 分析要求

### 分组原则
1. **功能相关性**: 根据业务功能的相关性进行分组
2. **合理规模**: 每个分组应包含2-多个（但最好不要超过10个）相关的说明书
3. **业务完整性**: 确保每个分组代表一个完整的业务流程或功能模块
4. **与已有分组的协调**: 考虑与之前批次聚合结果的关系，避免重复或冲突

### 聚合类型命名
- 使用简洁明了的中文名称
- 体现核心业务功能（如：订单流程、用户管理、支付流程等）
- 避免技术术语，专注业务语义
- 如果扩展已有分组，使用相同或相似的命名

### 相关性评分标准
- **90-100分**: 高度相关，属于同一核心业务流程
- **70-89分**: 中度相关，属于相关业务模块
- **50-69分**: 低度相关，有一定业务关联
- **50分以下**: 不建议分组

{if(isFirstBatch)}
### 首批特殊要求
- 这是第一批分析，请建立基础的聚合分组
- 重点关注业务功能的核心分类
- 为后续批次建立良好的分组基础
{endif}

{if(isLastBatch)}
### 最后一批特殊要求
- 这是最后一批分析，请确保分组的完整性
- 检查是否有遗漏的业务关联
- 确保所有重要的业务流程都得到了合理的聚合
{endif}

## 输出格式
请严格按照以下JSON格式返回结果，不要添加任何其他内容：

```json
[
  \{
    "aggregationType": "订单流程",
    "description": "包含订单创建、修改、取消等相关流程{if(!isFirstBatch)}，与之前批次的'订单管理'分组相关{endif}",
    {if(!isFirstBatch)}"historicalGroupId": "group_1234567890_123",  // 如果要合并到历史分组，设置此字段；如果是新分组，不要设置此字段{endif}
    "documentationSummaries": [
      \{
        "entryPointId": "...",
        "entryPointName": "...",
        "title": "...",
        "summary": "...",
        "documentationId": "..."
      \}
    ],
    "relevanceScore": 85
  \}
]
```

## 特别注意
- 只返回JSON数组，不要包含其他解释文字
- 确保每个分组至少包含当前批次中的1个说明书
- documentationId必须使用实际的文档ID，不要使用占位符
- 分组描述要具体，避免泛泛而谈
- 相关性评分要客观合理
{if(!isFirstBatch)}
- **关键**：如果当前批次的说明书应该归入历史分组，必须设置 `historicalGroupId` 字段
- **关键**：如果是新分组，不要设置 `historicalGroupId` 字段
- 不要设置 `groupId` 字段，系统会自动处理
- 充分考虑与之前批次聚合结果的关系，但不要重复返回之前批次的内容
- 只返回当前批次新产生的聚合分组或对已有分组的扩展
{endif}